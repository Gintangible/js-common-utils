{
  // 显示不同断言库特征。
  "diff": true,
  "extension": ["js"],
  // "package": "./package.json",
  // 测试报告的格式
  "reporter": "nyan",
  /* Mocha默认只执行test子目录下面第一层的测试用例，不会执行更下层的用例。
   * test子目录下面所有的测试用例----不管在哪一层----都会执行。
   */
  "recursive": true,
  // 高亮显示超过 75ms 的例子。
  "slow": 75,
  // 测试脚本的转码器
  "require": ["@babel/register", "esm", "jsdom-global/register"],
  "timeout": 5000,
  // 指定用户界面
  "ui": "bdd"
  // 监视指定的测试脚本。只要测试脚本有变化，就会自动运行Mocha
  // "watch": true
}
